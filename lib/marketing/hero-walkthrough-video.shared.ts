export const HERO_WALKTHROUGH_VIDEO_ID = "hero-walkthrough-video" as const

/** Matches the 1662x1080 footage so the slot reserves height before load. */
export const HERO_VIDEO_ASPECT_CLASS = "aspect-[1662/1080]"

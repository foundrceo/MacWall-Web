# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production / deploy
/build
.vercel

# turbo
.turbo

# misc
.DS_Store
*.pem
Thumbs.db

# browser / e2e tooling
.playwright-cli/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# env / secrets — never commit real values
.env
.env.*
!.env.example
.env.local
.env.development
.env.production
.env*.local
*.pem
credentials.json
*-credentials.json
service-account*.json
.secrets/

# local AI agent skills / lockfiles (never publish)
.agents/
skills/
skills-lock.json

# local backend source / private ops docs (deployed remotely)
supabase/
.supabase/

# history rewrite helpers / local bundles (never publish)
.git-scrub*.py
.git-rewrite*.py
*.bundle

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE (local only)
.idea/
.vscode/*
!.vscode/extensions.json
